type Post {
  id: ID!
  title: String
  slug: String
  summary: String
  cover: String
  icon: String
  published_date: String
  is_pinned: Boolean
  tags: [String]
  markdown: String
}

type Query {
  posts: [Post]
  post(slug: String!): Post
}
